{% extends 'base.html.twig' %}

{% block title %}Reserver l'annonce : {{ad.title}} {% endblock %}

{% block body %}
<div class="container">
    <h1>Réservez pour l'annonce : {{ ad.title }}</h1>

    <p>Vous vous êtes sur le point de réserver le bien de <strong>{{ ad.author.fullName }}</strong>, dites nous tout, on
        s'occupe du reste !
    </p>
    {{form_start(form)}}
    <div class="aler alert-light">
        <h2>Commencez par nous dire quelles sont les dates qui vous interessent !</h2>
    </div>
    <div class="row">
        <div class="col">
            {{ form_row(form.startDate) }}
        </div>
        <div class="col">
            {{form_row(form.endDate)}}
        </div>
        
    </div>

    <h4>Montant du séjour :  <span id="amount">...</span> &euro;</h4>
    <span id="days">0</span> nuits à {{ad.price}} / nuit

    {{ form_row(form.comment) }}

    <h2>Vous avez terminé !</h2>
    <p>Vacances de rêve ? Hébergement pour le boulot ? Peu importe, on est avec vous!</p>

    <button type="submit" class="btn btn-primary">Je réserve !</button>

    {{form_end(form)}}

</div>


{% endblock %}